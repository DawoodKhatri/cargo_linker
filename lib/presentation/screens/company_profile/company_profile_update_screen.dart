import 'package:flutter/material.dart';

class CompanyProfileUpdateScreen extends StatelessWidget {
  const CompanyProfileUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("CargoLinker"),
        centerTitle: true,
      ),
      body: const Placeholder(),
    );
  }
}
